import Grid from "@mui/material/Grid";
import Box from "@mui/material/Box";

// CURRENTLY NOT USED
// TODO: RESUABLE CARD
const StatModalCard = (props) => {
  return (
    <Box>
      <Grid>
        <Grid item xs={12}>
          Test
        </Grid>
      </Grid>
    </Box>
  );
};

export default StatModalCard;
