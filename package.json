{
  "name": "blackjack-gto",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=development && npx nodemon@latest server.js",
    "build": "rm -rf build && cd client && npm run build && mv temp_build ../build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "prettier": "2.7.1"
  }
}